#cloud-config
runcmd:
  - add-apt-repository -y ppa:zebr0/master
  - apt-get -y install zebr0-core
  - zebr0-init -u {{ url }} -p {{ project }} -s {{ stage }}
  - apt-get -y install idem
  - idem run aws
power_state:
  mode: reboot
